'use strict';



// Render the components
React.renderComponent(
    <Listapp />, 
    document.getElementById('thelist')
);

var s,
    ListWidget = {
    settings: {},

    init: function init() {
        
    },
};

ListWidget.init();